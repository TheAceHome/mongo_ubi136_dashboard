# 🚀 Быстрый старт UBI.136 Protection System

## За 3 минуты до рабочей системы

### 1️⃣ Запуск (одна команда)

```bash
./start.sh
```

или вручную:

```bash
docker-compose up -d
```

### 2️⃣ Проверка (15 секунд)

Дождитесь сообщения:
```
✅ Система успешно запущена!
```

### 3️⃣ Тестирование

```bash
./test_api.sh
```

или вручную:

```bash
# Проверка главного сервиса
curl http://localhost:8001/health

# Безопасная запись
curl -X POST http://localhost:8001/write/safe \
  -H "Content-Type: application/json" \
  -d '{
    "collection": "test",
    "document": {"message": "Hello, UBI.136 Protection!"},
    "write_concern": "majority"
  }'
```

---

## 📊 Доступные сервисы

| Сервис | URL | Описание |
|--------|-----|----------|
| Consensus (главный) | http://localhost:8001 | Управление согласованностью |
| Replication Monitoring | http://localhost:8002 | Мониторинг репликации |
| Health Check | http://localhost:8003 | Проверка узлов |
| Transaction Log | http://localhost:8004 | Логирование операций |
| Recovery Service | http://localhost:8005 | Восстановление узлов |

---

## 🧪 Демонстрация защиты от UBI.136

### Симуляция атаки (отключение узла)

```bash
# 1. Остановить Secondary узел
docker stop mongo-secondary1

# 2. Проверить алерты (система обнаружит проблему)
curl http://localhost:8002/monitoring/alerts

# 3. Попробовать безопасную запись (должна пройти!)
curl -X POST http://localhost:8001/write/safe \
  -H "Content-Type: application/json" \
  -d '{"collection":"test","document":{"test":"data"},"write_concern":"majority"}'

# 4. Восстановить узел
docker start mongo-secondary1

# 5. Проверить восстановление
curl http://localhost:8005/recovery/status
```

**Результат:** ✅ Данные защищены, система продолжает работать!

---

## 🎯 Основные API endpoints для демонстрации

### 1. Безопасная запись
```bash
curl -X POST http://localhost:8001/write/safe \
  -H "Content-Type: application/json" \
  -d '{"collection":"demo","document":{"msg":"Protected"},"write_concern":"majority"}'
```

### 2. Статус кластера
```bash
curl http://localhost:8003/health/summary
```

### 3. Анализ репликации
```bash
curl http://localhost:8002/replication/lag
```

### 4. Логи операций
```bash
curl http://localhost:8004/logs/recent
```

### 5. Рекомендации по восстановлению
```bash
curl http://localhost:8005/recovery/recommendations
```

---

## 📱 Использование Postman

1. Импортировать: `UBI136_Postman_Collection.json`
2. Запустить любой запрос из коллекции
3. Все endpoints готовы к использованию!

---

## 🛑 Остановка системы

```bash
docker-compose down
```

Полная очистка (с удалением данных):
```bash
docker-compose down -v
```

---

## 📚 Дополнительная документация

- **README.md** - Полная документация
- **ARCHITECTURE.md** - Детальная архитектура
- **test_api.sh** - Автоматические тесты

---

## ✅ Чеклист для защиты курсовой

- [ ] Запустил `./start.sh` - все 8 контейнеров работают
- [ ] Проверил все 5 сервисов через браузер
- [ ] Выполнил безопасную запись
- [ ] Симулировал отключение узла
- [ ] Показал работу мониторинга
- [ ] Продемонстрировал восстановление
- [ ] Объяснил механизмы защиты

---

**Готово! Система защищает от UBI.136 🛡️**
